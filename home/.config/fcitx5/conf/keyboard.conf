# fcitx5 の keyboard エンジン (keyboard-us) の設定。
# Hint は英字入力中に単語補完の候補窓を出す機能で、既定のトリガーが Ctrl+Alt+H と
# Ctrl+Alt+J。Magnet 風の四分割 (Ctrl+Alt+U/I/J/K、xremap.nix) の隣なので押し間違えると
# 英語モードなのに変換候補のような窓が出続ける。トリガーを外し、既定でも無効にする。
EnableHintByDefault=False
Hint Trigger=
One Time Hint Trigger=
