onboarding = false
# herdr config — https://herdr.dev/docs/configuration/
# Only overrides are listed; everything else stays at herdr's defaults.

[keys]
# 指馴染みで prefix は C-w のまま (herdr default: ctrl+b)。
prefix = "ctrl+w"

# tab 切替は prefix なしの C-Tab / C-BTab も併用する
# (default の prefix+n / prefix+p も配列で残す)。
# 注: C-Tab を届けるには WezTerm 側の CSI-u 設定が必要で、herdr が
#     そのシーケンスを解釈するかは要確認 (効かなければこの2行を消す)。
next_tab = ["prefix+n", "ctrl+tab"]
previous_tab = ["prefix+p", "ctrl+shift+tab"]

# pane 移動は矢印のみ (herdr default の hjkl は外す)
focus_pane_left = "prefix+left"
focus_pane_down = "prefix+down"
focus_pane_up = "prefix+up"
focus_pane_right = "prefix+right"

# 共通ピッカー tuicast (worktree / ssh)。zsh 側の C-] と同じ入口で、
# herdr 内外の挙動差は tuicast の run スクリプト側が吸収する。
# レイアウトを崩さない floating popup で出し、選択が済めば自動で閉じる。
[[keys.command]]
key = "ctrl+]"
type = "popup"
command = "tuicast"
description = "tuicast (popup)"
width = "90%"
height = "90%"

[ui]
agent_panel_sort = "spaces"

# サイドバーを既定(26)の約1.5倍に。名前長で min..max の範囲を自動スケールするので、
# 短い名前だと下限まで縮むので、下限自体を 39 に上げて実質固定する(上限も併せて上げる)。
sidebar_min_width = 39
sidebar_width = 39
sidebar_max_width = 45

# モバイル端末で幅が広めでもサイドバーを畳んで単一カラムにしたい。
# この桁数以下だとモバイルレイアウト(サイドバー非表示)になる。default は 64。
# iPhone 17 の Termius 最小フォントサイズで幅 81 桁だったので、それを確実に含むよう
# 少し余裕を持たせて 100 に設定 (フォント調整で幅が増えても判定が外れにくい)。
mobile_width_threshold = 100

# tmux の prefix+c のように、名前入力なしで即 tab を作る。
prompt_new_tab_name = false

# default は [["state_icon","workspace","tab"],["agent"]]。
# 1行目は状態+workspace、2行目に agent と tab、3行目に実行中タスク
# (terminal_title_stripped)。state_text (icon と冗長) / terminal_title
# (stripped で足りる) / pane は不採用。
[ui.sidebar.agents]
# 2-3行目は素の状態だと dim で薄すぎたので明示的に解除する。
rows = [
  ["state_icon", "workspace"],
  [{ token = "agent", dim = false }, { token = "tab", dim = false }],
  [{ token = "terminal_title_stripped", dim = false }],
]

[update]
# バイナリは aqua(+Renovate) で管理するので、herdr 自身の版チェック/自己更新は無効化。
# (`herdr update` は叩かない。aqua 管理とドリフトするため)
version_check = false
# manifest_check はデフォルト(true)のまま: agent 検出ルールは本体更新と別軸で最新化される。

[experimental]
# prefix モード中だけ macOS の入力ソースを ASCII に一時切替。日本語 IME オン中でも
# prefix コマンド (prefix→c 等) が IME に食われず通る。抜けたら元の入力ソースに戻す。
switch_ascii_input_source_in_prefix = true
# kitty graphics protocol を pane に通す (icat 等での画像表示)。これがないと
# ピクセルサイズ問い合わせ (CSI 14 t) に herdr が応答せず icat がエラーになる。
kitty_graphics = true

[theme]
name = "vesper"
auto_switch = false
